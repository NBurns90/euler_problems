require 'spec_helper'

describe Problem do

  before(:each) do
    test = Problem.new
  end

  # it "should check if number is even and push to evenarray" do
  #   expect(test.iterator(1,2)).to eq([10])
  # end

  # it "should check array contains fib numbers" do
  #   expect(Problem.new.iterator(1,2).num_arr.to include(1, 2, 3, 5, 8, 13)
  # end

end
